import { Megaphone, BookOpen, Sparkles, TrendingUp, MonitorSmartphone, Building2 } from "lucide-react";

// Maps the `icon` string on each service to its lucide component
export const SERVICE_ICONS = {
  Megaphone,
  BookOpen,
  Sparkles,
  TrendingUp,
  MonitorSmartphone,
  Building2,
};
